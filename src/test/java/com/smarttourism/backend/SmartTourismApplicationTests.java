package com.smarttourism.backend;

import org.junit.jupiter.api.Test;

/**
 * Smoke test — verifies the Spring application context loads correctly.
 * Full integration tests (with Testcontainers) live in the test profile.
 */
class SmartTourismApplicationTests {

    @Test
    void contextLoads() {
        // Context load is verified by the @SpringBootTest annotation in integration tests.
        // This placeholder ensures the test suite compiles and runs without errors.
    }
}
